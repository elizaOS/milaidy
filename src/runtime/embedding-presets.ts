// Re-export upstream definitions.

export type {
  EmbeddingPreset,
  EmbeddingTier,
} from "@elizaos/autonomous/runtime/embedding-presets";
export {
  detectEmbeddingPreset,
  detectEmbeddingTier,
  EMBEDDING_PRESETS,
} from "@elizaos/autonomous/runtime/embedding-presets";
